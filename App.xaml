<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Vidvix.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vidvix">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                    <SolidColorBrush x:Key="SettingsPaneBackgroundBrush" Color="#FFF8F8F8" />
                    <SolidColorBrush x:Key="SettingsPaneSectionBrush" Color="#FFFFFFFF" />
                    <SolidColorBrush x:Key="SettingsPaneBorderBrush" Color="#1A000000" />
                    <SolidColorBrush x:Key="SettingsPaneBackdropBrush" Color="#12000000" />
                    <SolidColorBrush x:Key="DetailOverlayBackgroundBrush" Color="#FFF7F7F7" />
                    <SolidColorBrush x:Key="DetailOverlaySectionBrush" Color="#FFFFFFFF" />
                    <SolidColorBrush x:Key="DetailOverlayBorderBrush" Color="#16000000" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="SettingsPaneBackgroundBrush" Color="#FFF8F8F8" />
                    <SolidColorBrush x:Key="SettingsPaneSectionBrush" Color="#FFFFFFFF" />
                    <SolidColorBrush x:Key="SettingsPaneBorderBrush" Color="#1A000000" />
                    <SolidColorBrush x:Key="SettingsPaneBackdropBrush" Color="#12000000" />
                    <SolidColorBrush x:Key="DetailOverlayBackgroundBrush" Color="#FFF7F7F7" />
                    <SolidColorBrush x:Key="DetailOverlaySectionBrush" Color="#FFFFFFFF" />
                    <SolidColorBrush x:Key="DetailOverlayBorderBrush" Color="#16000000" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="SettingsPaneBackgroundBrush" Color="#FF1E1E1E" />
                    <SolidColorBrush x:Key="SettingsPaneSectionBrush" Color="#FF2A2A2A" />
                    <SolidColorBrush x:Key="SettingsPaneBorderBrush" Color="#26FFFFFF" />
                    <SolidColorBrush x:Key="SettingsPaneBackdropBrush" Color="#18000000" />
                    <SolidColorBrush x:Key="DetailOverlayBackgroundBrush" Color="#FF181818" />
                    <SolidColorBrush x:Key="DetailOverlaySectionBrush" Color="#FF232323" />
                    <SolidColorBrush x:Key="DetailOverlayBorderBrush" Color="#29FFFFFF" />
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="SettingsPaneBackgroundBrush" Color="#FF000000" />
                    <SolidColorBrush x:Key="SettingsPaneSectionBrush" Color="#FF000000" />
                    <SolidColorBrush x:Key="SettingsPaneBorderBrush" Color="#FFFFFFFF" />
                    <SolidColorBrush x:Key="SettingsPaneBackdropBrush" Color="#00000000" />
                    <SolidColorBrush x:Key="DetailOverlayBackgroundBrush" Color="#FF000000" />
                    <SolidColorBrush x:Key="DetailOverlaySectionBrush" Color="#FF000000" />
                    <SolidColorBrush x:Key="DetailOverlayBorderBrush" Color="#FFFFFFFF" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>

            <Style
                x:Key="SurfaceCardBorderStyle"
                TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="20" />
            </Style>

            <Style
                x:Key="SubtleBadgeBorderStyle"
                TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="14" />
            </Style>

            <Style
                x:Key="WorkspaceModeToggleButtonStyle"
                TargetType="ToggleButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="16" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="MinWidth" Value="60" />
                <Setter Property="MinHeight" Value="72" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid
                                x:Name="LayoutRoot"
                                MinHeight="{TemplateBinding MinHeight}"
                                MinWidth="{TemplateBinding MinWidth}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver" />
                                        <VisualState x:Name="Pressed" />
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetName="LayoutRoot"
                                                    Storyboard.TargetProperty="Opacity"
                                                    To="0.45"
                                                    Duration="0" />
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>

                                    <VisualStateGroup x:Name="CheckStates">
                                        <VisualState x:Name="Unchecked" />
                                        <VisualState x:Name="Checked" />
                                        <VisualState x:Name="Indeterminate" />
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border
                                    x:Name="InteractionSurface"
                                    Margin="0,4,0,4"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    Padding="10,10">
                                    <ContentPresenter
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}" />
                                </Border>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style
                x:Key="MergeModeSegmentRadioButtonStyle"
                TargetType="RadioButton">
                <Setter Property="MinWidth" Value="144" />
                <Setter Property="MinHeight" Value="58" />
                <Setter Property="Padding" Value="14,10" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="UseSystemFocusVisuals" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Grid
                                x:Name="LayoutRoot"
                                MinHeight="{TemplateBinding MinHeight}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="InteractionSurface.Opacity" Value="0.96" />
                                                <Setter Target="InteractionSurface.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="InteractionSurface.Opacity" Value="0.9" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Opacity" Value="0.45" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>

                                    <VisualStateGroup x:Name="CheckStates">
                                        <VisualState x:Name="Unchecked">
                                            <VisualState.Setters>
                                                <Setter Target="InteractionSurface.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                                <Setter Target="InteractionSurface.BorderBrush" Value="Transparent" />
                                                <Setter Target="SelectionIndicator.Opacity" Value="0" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Checked">
                                            <VisualState.Setters>
                                                <Setter Target="InteractionSurface.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                                <Setter Target="InteractionSurface.BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                                <Setter Target="SelectionIndicator.Opacity" Value="1" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Indeterminate">
                                            <VisualState.Setters>
                                                <Setter Target="InteractionSurface.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                                <Setter Target="InteractionSurface.BorderBrush" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                                                <Setter Target="SelectionIndicator.Opacity" Value="1" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Border
                                    x:Name="InteractionSurface"
                                    Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="16"
                                    Opacity="1"
                                    Padding="{TemplateBinding Padding}">
                                    <ContentPresenter
                                        x:Name="ContentHost"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        ContentTransitions="{TemplateBinding ContentTransitions}" />
                                </Border>
                                <Border
                                    x:Name="SelectionIndicator"
                                    Height="3"
                                    Width="34"
                                    Margin="0,0,0,5"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Bottom"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="999"
                                    Opacity="0" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style
                x:Key="SectionTitleTextStyle"
                TargetType="TextBlock">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style
                x:Key="CaptionTextStyle"
                TargetType="TextBlock">
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Opacity" Value="0.68" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
