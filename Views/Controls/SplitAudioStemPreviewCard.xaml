<UserControl
    x:Class="Vidvix.Views.Controls.SplitAudioStemPreviewCard"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:converters="using:Vidvix.Views.Converters"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style
            x:Key="TimelineStripButtonStyle"
            TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="MinWidth" Value="30" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid
                            x:Name="LayoutRoot"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="InteractionSurface.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="InteractionSurface.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Disabled">
                                        <VisualState.Setters>
                                            <Setter Target="LayoutRoot.Opacity" Value="0.42" />
                                            <Setter Target="InteractionSurface.Background" Value="Transparent" />
                                            <Setter Target="InteractionSurface.BorderBrush" Value="Transparent" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>

                            <Border
                                x:Name="InteractionSurface"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <converters:TimelineMillisecondsToTimeConverter x:Key="TimelineMillisecondsToTimeConverter" />
    </UserControl.Resources>

    <Border
        Padding="12"
        Background="{ThemeResource ControlFillColorTertiaryBrush}"
        BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"
        BorderThickness="1"
        CornerRadius="14">
        <Grid RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="4">
                    <TextBlock FontWeight="SemiBold" Text="{x:Bind StemTitle, Mode=OneWay}" />
                    <TextBlock
                        Opacity="0.72"
                        Text="{x:Bind DisplayFileName, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <Button
                    Grid.Column="1"
                    Command="{x:Bind RevealCommand, Mode=OneWay}"
                    CommandParameter="{x:Bind FilePath, Mode=OneWay}"
                    Content="{x:Bind RevealButtonText, Mode=OneWay}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top" />
            </Grid>

            <Grid
                Grid.Row="1"
                ColumnSpacing="10"
                VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    AutomationProperties.Name="{x:Bind PlayPauseButtonText, Mode=OneWay}"
                    Click="OnPlayPauseClick"
                    IsEnabled="{x:Bind CanPlayPreview, Mode=OneWay}"
                    Style="{StaticResource TimelineStripButtonStyle}"
                    ToolTipService.ToolTip="{x:Bind PlayPauseButtonText, Mode=OneWay}">
                    <SymbolIcon
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Symbol="{x:Bind PlayPauseButtonSymbol, Mode=OneWay}" />
                </Button>

                <Slider
                    Grid.Column="1"
                    x:Name="TimelineSlider"
                    Height="24"
                    IsEnabled="{x:Bind CanPlayPreview, Mode=OneWay}"
                    IsThumbToolTipEnabled="True"
                    Margin="0,-3,0,0"
                    ThumbToolTipValueConverter="{StaticResource TimelineMillisecondsToTimeConverter}"
                    VerticalAlignment="Center"
                    Background="{ThemeResource ControlFillColorSecondaryBrush}"
                    CornerRadius="999"
                    Foreground="{ThemeResource ControlStrongFillColorDefaultBrush}"
                    Maximum="{x:Bind TimelineMaximum, Mode=OneWay}"
                    Minimum="{x:Bind TimelineMinimum, Mode=OneWay}"
                    Value="{x:Bind CurrentPositionMilliseconds, Mode=TwoWay}"
                    ValueChanged="OnTimelineSliderValueChanged">
                    <Slider.Resources>
                        <x:Double x:Key="SliderHorizontalHeight">24</x:Double>
                        <x:Double x:Key="SliderTrackThemeHeight">4</x:Double>
                        <x:Double x:Key="SliderHorizontalThumbWidth">12</x:Double>
                        <x:Double x:Key="SliderHorizontalThumbHeight">12</x:Double>
                        <StaticResource x:Key="SliderThumbBackground" ResourceKey="ControlStrongFillColorDefaultBrush" />
                        <StaticResource x:Key="SliderThumbBackgroundPointerOver" ResourceKey="TextFillColorPrimaryBrush" />
                        <StaticResource x:Key="SliderThumbBackgroundPressed" ResourceKey="TextFillColorPrimaryBrush" />
                    </Slider.Resources>
                </Slider>

                <TextBlock
                    Grid.Column="2"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Style="{StaticResource CaptionTextStyle}"
                    Text="{x:Bind TimelinePositionText, Mode=OneWay}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
