<UserControl
    x:Class="Vidvix.Views.Controls.DualThumbRangeSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:primitives="using:Microsoft.UI.Xaml.Controls.Primitives"
    mc:Ignorable="d">

    <Grid
        x:Name="LayoutRoot"
        Height="44"
        SizeChanged="OnLayoutRootSizeChanged">
        <Canvas x:Name="TrackCanvas">
            <Border
                x:Name="TrackBackground"
                Height="6"
                Background="{ThemeResource ControlFillColorTertiaryBrush}"
                CornerRadius="3"
                Canvas.Top="19" />

            <Border
                x:Name="SelectedRangeBar"
                Height="6"
                Background="{ThemeResource AccentFillColorDefaultBrush}"
                CornerRadius="3"
                Canvas.Top="19" />

            <primitives:Thumb
                x:Name="StartThumb"
                Width="18"
                Height="18"
                Canvas.Top="13"
                DragDelta="OnStartThumbDragDelta">
                <primitives:Thumb.Template>
                    <ControlTemplate TargetType="primitives:Thumb">
                        <Grid
                            Width="18"
                            Height="18">
                            <Ellipse
                                Fill="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                                Stroke="{ThemeResource AccentFillColorDefaultBrush}"
                                StrokeThickness="2" />
                        </Grid>
                    </ControlTemplate>
                </primitives:Thumb.Template>
            </primitives:Thumb>

            <primitives:Thumb
                x:Name="EndThumb"
                Width="18"
                Height="18"
                Canvas.Top="13"
                DragDelta="OnEndThumbDragDelta">
                <primitives:Thumb.Template>
                    <ControlTemplate TargetType="primitives:Thumb">
                        <Grid
                            Width="18"
                            Height="18">
                            <Ellipse
                                Fill="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                                Stroke="{ThemeResource AccentFillColorDefaultBrush}"
                                StrokeThickness="2" />
                        </Grid>
                    </ControlTemplate>
                </primitives:Thumb.Template>
            </primitives:Thumb>
        </Canvas>
    </Grid>
</UserControl>
